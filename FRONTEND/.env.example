# Laisser vide : le navigateur appelle /api/...
# - en local  → proxy Vite (vite.config.js) vers le backend
# - sur Vercel → rewrite vercel.json vers Lightsail
#
# Pour viser l'API directement (HTTPS + CORS obligatoires) :
# VITE_API_BASE_URL=https://ton-api.exemple
VITE_API_BASE_URL=
